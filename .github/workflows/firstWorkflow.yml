name: First Workflow

run-name: ${{github.actor}}

on: workflow_dispatch

jobs:
 job1:
  runs-on: ubuntu-latest
  steps: 
    - run: echo "display the workflow for ubuntu"
 job2:
  runs-on: ubuntu-latest
  steps: 
    - run: echo "display the workflow for ubuntu-2"
